import useComGndModule from "./use-com-gnd-bt-module";

/**
 * Read a sensor value at a set interval
 * @param {int} interval time between readings
 */
export default function useComGndBtModuleMonitor(interval){

}